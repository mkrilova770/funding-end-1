# Template for Railway: Variables tab → RAW Editor → paste, then fill secrets.
# Or use "Suggested variables" / repo scan (Railway picks up .env.* in repo root).

DATABASE_URL=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
SYNC_INTERVAL_MS=45000
SYNC_HISTORY_CONCURRENCY=6
TELEGRAM_SPREAD_DIGEST_ENABLED=0
TELEGRAM_DIGEST_UI_SECRET=
NODE_ENV=production
